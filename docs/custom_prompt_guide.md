# 自定义提示符使用指南

## ✅ 配置完成

您的zsh提示符已配置为以下风格：

```
# username @ hostname in ~/path on git:branch o .venv [HH:MM:SS]
$ 
```

## 🎨 提示符说明

- **# username** - 蓝色#号 + 绿色用户名
- **@ hostname** - @符号 + 黄色主机名
- **in ~/path** - 青色当前目录路径
- **on git:branch** - 紫色git标签 + 红色分支名（仅在git仓库中显示）
- **o .venv** - 黄色o + 绿色虚拟环境名（如果激活了虚拟环境）
- **[HH:MM:SS]** - 蓝色时间戳（24小时制）

## 🚀 使用方法

### 方法1: 重新加载配置（当前shell）

```bash
source ~/.zshrc
```

### 方法2: 重新激活zsh

```bash
exit  # 退出当前zsh
zsh-activate  # 或 source ~/bin/activate-zsh
```

## 📝 配置位置

配置文件：`~/.zshrc`

关键配置部分：
```bash
# 自定义提示符配置（在.zshrc文件末尾）
precmd_custom() {
    # 构建提示符...
}
```

## 🔧 自定义修改

### 修改颜色

编辑 `~/.zshrc`，找到颜色代码：

- `%F{blue}` - 蓝色
- `%F{green}` - 绿色
- `%F{yellow}` - 黄色
- `%F{cyan}` - 青色
- `%F{magenta}` - 紫色
- `%F{red}` - 红色
- `%F{white}` - 白色

### 修改时间格式

当前使用 `%*` (HH:MM:SS)，可以改为：
- `%T` - HH:MM (24小时制)
- `%t` - HH:MM (12小时制，AM/PM)
- `%D{%Y-%m-%d %H:%M:%S}` - 完整日期时间

### 隐藏某些部分

如果不想显示某些信息，可以注释掉相应行：

```bash
# 不显示git信息
# if command git rev-parse --git-dir > /dev/null 2>&1; then
#     ...
# fi

# 不显示虚拟环境
# local venv_info=$(venv_prompt_info)
# if [ -n "$venv_info" ]; then
#     ...
# fi
```

## 🧪 测试提示符

1. **进入git仓库测试**：
```bash
cd ~/minimind
# 应该显示: on git:master 或 on git:branch-name
```

2. **激活虚拟环境测试**：
```bash
source ~/minimind/.venv/bin/activate
# 应该显示: o .venv 或 o minimind
```

3. **切换目录测试**：
```bash
cd ~
cd /tmp
# 路径应该实时更新
```

## 📚 参考

- [Zsh提示符文档](http://zsh.sourceforge.net/Doc/Release/Prompt-Expansion.html)
- [Oh-My-Zsh主题](https://github.com/ohmyzsh/ohmyzsh/wiki/Themes)

---

**配置日期**: $(date +%Y-%m-%d)

