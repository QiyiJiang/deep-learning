# llm_study 环境变量配置示例
# 复制此文件为 .env 并根据需要修改

# 日志配置
LOG_LEVEL=INFO
# LOG_FILE=logs/llm_study.log
# LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# 训练配置（可选，会覆盖 config.py 中的默认值）
# DEFAULT_LR=1e-4
# DEFAULT_BATCH_SIZE=1
# DEFAULT_CHECKPOINTS_DIR=checkpoints

# 数据路径（可选）
# DEFAULT_PRETRAIN_DATA_PATH=dataset/pretrain_hq.jsonl
# DEFAULT_SFT_DATA_PATH=dataset/sft_mini_512_15.jsonl

# CUDA 设备（可选）
# CUDA_DEVICE=cuda:0
